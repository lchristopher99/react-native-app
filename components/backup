import React from 'react';
import {
  View,
  StyleSheet,
} from 'react-native';
import {
  Button,
} from 'react-native-elements';

const FetchOthersLocation = props => {
  return (
    {/* Below is the get other users locations button */}
    <View>
      <Button
        title="Get Other User Locations!"
        // onPress={this.otherUsersHandler} // once clicked, this button activates the following handler: this.otherUsersHandler - located above
        icon={{name: 'users', type: 'font-awesome'}}
        buttonStyle={{
          marginBottom: 30,
          backgroundColor: 'rgba(92, 99,216, 1)',
          width: 300,
          height: 45,
          borderColor: "transparent",
          borderWidth: 0,
          borderRadius: 5
        }}
      />  
    </View>
  );
}

export default FetchOthersLocation;